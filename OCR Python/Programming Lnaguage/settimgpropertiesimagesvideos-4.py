import cv2 
import numpy as np 

'''
we can make the width and the height using 
print(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
for height also 

we can make the width and the height as : 
cap.set(3,1208) To set the width and for height the parameter is 4 this make the height and widht of the video to some extent 

make the date variable and check the date usign the python datetime module and then cv2.puttext(date) to put the date and time in the current video
same for the Images too 



'''